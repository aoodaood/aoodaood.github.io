@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div>
        @using (Html.BeginForm("Index", "Home", FormMethod.Post))
        {
            <div class="col-md-3">
                <input placeholder="ใส่เบอร์โทรศัพท์" style="width:200px; height:30px; font-size:medium; margin-top:10px;text-align:left;padding-left:5px;" type="text" name="input" value="@Session["Input"]" />
                <input style="width:120px; height:30px; font-size:medium; border-radius:3px 4px 5px; margin-top:15px; text-align:left;padding-right:15px;margin-top:20px;" name="saving" type="submit" value="OK" />
                <div style="padding-top:10px">@Session["Result"]</div>
            </div>
            
        }
    </div>
</div>
